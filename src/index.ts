import { GutenbergStyles } from "../types";
import Root from "./components";

const gutenbergStyles: GutenbergStyles = {
  name: "frontity-gutenberg-styles",
  roots: {
    gutenbergStyles: Root,
  },
};

export default gutenbergStyles;
